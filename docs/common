

- TABLES
    - namespaces
        - id - randomUUID
        - name - unique - по умолчанию default
        - status - ENABLED/DISABLED

    - filters
        - id - randomUUID
        - name - ui формирует автоматически подставляя "$parameter $operator" - уникальный (можно сменить)
        - namespace - KEY - внешний ключ на namespaces.id
        - parameter - KEY - имя параметра, который будет передаваться в json пользователем
        - operator - KEY - eq more less listEq
        - description - описание фильтра, пользователь придумывает самостоятельно

    - features
        - id - randomUUID
        - namespace - внешний ключ на namespaces.id
        - name - имя фичи (должно быть уникальным)
        - filters - json object следующего вида
            - LIST
                - id - внешний ключ на filters.id
                - value
        - status - ENABLED/DISABLED
        - description - описание фичи, пользователь придумывает самостоятельно

    - versions
        - id
        - namespace
        - version

- FEATURES
    - неймспейсы
        - должен быть дефолтный неймспейс
        - для каждого неймспейса свой поисковый индекс
        - удаление неймспейсов
    - фильтры
        - создание
        - удаление (чем меньше фильтров, тем быстрее работает поиск)

    - конфиги
        - возможность выгрузки в json и из json
    - кэширование
        - продумать систему кэширования

- USER FLOW
    - пользователь создает неймспейс или использует дефолтный
    - переходит в необходимый неймспейс (далее он будет использоваться по умолчанию)
    - пользователь создает необходимые фильтры (связка parameter/operator должны быть уникальными)
        - фильтр по имени пользователя (пользователем задаются только следующие параметры, остальное проставляется автоматически)
            - parameter: userName
            - operator: listEq
            - description: Фильтр по имени пользователя (можно внести список имен пользователей, на которые будет распространена фича, спецсимволы применять нельзя)
        - фильтр по типу os
            - parameter: os
            - operator: eq
            - description: Фильтр по типу операционной системы
        - фильтр по версии os
            - parameter: osVersion
            - operator: more
            - description: Фильтр по версии операционной системы (версия пользователя должна быть больше чем заданная в фильтре)
    - ! все три фильтра после создания будут использоваться при поиске, даже если они не прописаны в ФТ (чем меньше фильтров, тем быстрее работает поиск)
    - пользователь видит список фильтров
        - userName listEq
        - os eq
        - osVersion more
    - пользователь создает необходимый для него ФТ и присваивает фильтры
        - создаются ФТ с параметрами (список фильтров)
            - name: newFeature
            - filters:
                - фильтр 1
                    - id: пользователь выбирает имя (userName listEq) из выпадающего списка, id подставляется автоматически
                    - value: "userName1 userName2"
                - фильтр 2
                    - id: пользователь выбирает имя (os eq) из выпадающего списка, id подставляется автоматически
                    - value: "ios"
                - фильтр 3
                    - id: пользователь выбирает имя (osVersion more) из выпадающего списка, id подставляется автоматически
                    - value: 12
            - status: ENABLED
            - description: Новая фича с названием newFeature для параметров userName os osVersion
    - система обновляет индексы через время указанное в конфигах
    - пользователь присылает запрос на эндпоинт и получает ФТ newFeature в ответ
        - userName: userName1
        - os: ios
        - osVersion: 13
    - пользователь присылает запрос на эндпоинт и не получает ФТ в ответ
            - userName: userName1
            - os: ios
            - osVersion: 11

- SERVER FLOW